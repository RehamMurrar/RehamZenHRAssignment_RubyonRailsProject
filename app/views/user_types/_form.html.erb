<%= form_with(model: user_type) do |form| %>
  <% if user_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_type.errors.count, "error") %> prohibited this user_type from being saved:</h2>

      <ul>
        <% user_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_type_id, style: "display: block" %>
    <%= form.number_field :user_type_id %>
  </div>

  <div>
    <%= form.label :user_type_name, style: "display: block" %>
    <%= form.text_field :user_type_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
